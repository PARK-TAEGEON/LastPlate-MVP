# run.py reads the process environment. This file is documentation, not auto-loaded.
LASTPLATE_DB_PATH=data/lastplate.db
LASTPLATE_MODE=demo
LASTPLATE_STAGE_TIMEOUT=120
LASTPLATE_DEBUG=false
# Only set a private token when explicitly enabling developer tools.
# LASTPLATE_DEBUG_TOKEN=your-private-token
